L1= [1,3,6,78,35,88,55] 
for element in L1:
    L2=list(reversed(L1))
    print(L2)
#L = [8, 24, 48, 2, 16]
#L2 = []

# Parcourir la liste d'origine en ordre inverse et ajouter les éléments à la nouvelle liste
#for i in range(len(L) - 1, -1, -1): 
          #range une fonction qui genere seqence d'entier Elle est couramment utilisée dans les boucles for pour créer une séquence d'indices ou de valeurs à parcourir.
#           ##len(L) sert a déterminer la longueur (le nombre d'éléments) d'une séquence, telle qu'une liste, un tuple, ou une chaîne de caractères. 
   # L2.append(L[i])
  #  print(L2)
