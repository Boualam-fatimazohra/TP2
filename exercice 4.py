L = [1, -30, 0, -2, 500, 4, 2, 100]

# Afficher les nombres négatifs
for element in L:
    if element < 0:
        print(element)

# Afficher les nombres positifs
for element in L:
    if element >= 0:
        print(element)
